# Fixture for this crate's doc tests and unit tests, not a stack meant to be run.
services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: composed
      POSTGRES_PASSWORD: composed
      POSTGRES_USER: composed
